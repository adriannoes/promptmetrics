# Frontend (Vite) - do not commit real secrets. Copy to .env.local and fill in.
# All external endpoints must use HTTPS/TLS in production.

# Supabase public URL and anon key (publishable)
VITE_SUPABASE_URL="https://<your-project-ref>.supabase.co"
VITE_SUPABASE_ANON_KEY="<your-anon-key>"

# Optional diagnostics: disable realtime to force polling-only on Home/Analysis
# You can also set this at runtime via localStorage.setItem("VITE_DISABLE_REALTIME","true")
VITE_DISABLE_REALTIME="false"

# Edge Functions / Integrations (configured as Supabase Function secrets in production)
# Never hardcode these in the frontend. Only used for local dev fallbacks when explicitly required.
# For local or dev, prefer setting via Supabase CLI secrets or project dashboard.
N8N_WEBHOOK_URL="https://your-n8n.example.com/webhook/analysis"
WAITLIST_WEBHOOK_URL="https://your-waitlist.example.com/webhook"

# Browser-only hints (non-sensitive)
VITE_APP_ENV="development"
VITE_LOG_LEVEL="info"
