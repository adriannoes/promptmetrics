###############################################
# env.example (documentação) — NÃO COMMITAR SEGREDOS REAIS #
###############################################

# Este arquivo documenta as variáveis esperadas pela aplicação.
# Copie este conteúdo para um arquivo `.env` na raiz do projeto localmente
# e preencha os valores conforme seu ambiente.
# Use sempre HTTPS/TLS em integrações externas. Nunca exponha chaves secretas no front-end.

# ---------------------------------------------
# Front-end (Vite) — chaves públicas
# ---------------------------------------------
# URL do projeto Supabase (ex.: https://<ref>.supabase.co)
VITE_SUPABASE_URL=

# Chave anônima (publishable) do Supabase — pública, porém trate com cuidado.
# Em produção, preferir injetar via variáveis de ambiente do provedor de hosting.
VITE_SUPABASE_ANON_KEY=

# Diagnóstico: quando "true", desabilita Realtime e usa apenas polling/snapshot
VITE_DISABLE_REALTIME=

# ---------------------------------------------
# Server/Edge Functions (NÃO expor no front)
# ---------------------------------------------
# Segredo para validação de HMAC em `receive-analysis` (n8n → Edge)
# Mantenha APENAS no ambiente do servidor (Edge Functions/Supabase), nunca no front.
N8N_HMAC_SECRET=

# Dicas de segurança:
# - Não commit ar `.env` com valores reais.
# - Prefira variáveis de ambiente no provedor (Vercel/Netlify/Fly/etc.).
# - Todas as chamadas externas devem usar HTTPS/WSS.


